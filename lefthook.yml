commit-msg:
  commands:
    validar-formato:
      run: |
        grep -qE '^(feat|fix|docs|style|refactor|perf|test|build|ci|chore|revert)(\(.+\))?: .{1,72}' {1} || (echo "❌ Error: El commit debe seguir Conventional Commits (ej: feat: añade panel)" && exit 1)
